@page
@model BeFeira.Pages.Feira.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Feira[0].Categoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Feira) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FeiraId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FeiraId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FeiraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
