@page
@model BeFeira.Pages.Subcategoria.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subcategoria[0].Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subcategoria[0].StandId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Subcategoria) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StandId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SubCategoriaId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SubCategoriaId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SubCategoriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
